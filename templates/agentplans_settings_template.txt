# agentPlans control file (auto-generated by the TSM panel TripList2Table tool).
# C++ port of 2_Create_LDT_TripTable + 3_get_ELTOD_TripTable. Placeholders in
# {braces} are filled from the panel/project settings (input paths come from the
# dialog's per-file rows, so manual overrides are honored).

# --- scenario / run ---
catalog_dir   = {catalog_dir}
scenario_dir  = {scenario_dir}
year          = {year}
feedback_loop = {feedback_loop}
seed          = 1

# --- temporal resolution ---
output_resolution      = {output_resolution}
sdt_input_resolution   = 30
ldt_input_resolution   = daily
truck_input_resolution = daily

# --- SDT / LDT / truck trip inputs ---
sdt_res_trips = {sdt_res_trips}
sdt_vis_trips = {sdt_vis_trips}
fl_ldt_tours  = {fl_ldt_tours}
os_ldt_tours  = {os_ldt_tours}
truck_odme    = {truck_odme}

# --- lookups (xlsx converted to CSV; see agentPlans docs/CSV_INPUTS.md) ---
tod_distributions    = {tod_distributions}
external_auto_shares = {external_auto_shares}
airport_shares       = {airport_shares}
canaveral_cruise     = {canaveral_cruise}
ga_al_destinations   = {ga_al_destinations}
cbm_external_lookup  = {cbm_external_lookup}
taz_dma              = {taz_dma}
sdt_syn_hh           = {sdt_syn_hh}

# Outputs:
#  trip_table_out   = the per-trip HyDRA/AgentFlow list (gz, depart_time at the
#                     selected resolution).
#  hourly_table_out = the hourly OD trip table CSV (wide by market, STARTTIME
#                     HH:00) -- same as the original R 3_get_ELTOD output.
trip_table_out   = {trip_table_out}
hourly_table_out = {scenario_dir}/ELTOD_tt_HourClock.csv
write_hourly_table = true

# --- external-station target calibration ---
# Scales OD trips serving each ext_zone_id to target / modeled. The targets file
# is written by the GUI; ext_station_* MUST match its ext_zone_id values.
apply_external_targets = true
ldt_external_targets   = {ldt_external_targets}
external_base_year     = {external_base_year}
ext_station_i10 = {ext_station_i10}
ext_station_i75 = {ext_station_i75}
ext_station_i95 = {ext_station_i95}

# --- toggles ---
apply_originState_based_externals = true
use_cbm_external_lookup = true
separate_res_markets    = true
track_AirTours          = true
